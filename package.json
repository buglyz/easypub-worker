{
  "name": "easypub-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build:local": "node scripts/build-local.mjs",
    "predev": "npm run build:local",
    "dev": "wrangler dev",
    "predeploy": "npm run build:local",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "fflate": "~0.8.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "esbuild": "^0.25.12",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9",
    "wrangler": "^4.123.0"
  }
}
